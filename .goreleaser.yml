version: 2

builds:
  - skip: true

release:
  github:
    owner: GoCodeAlone
    name: libsignal-service-go

archives:
  - formats: [tar.gz]
    files:
      - LICENSE
      - README.md
      - internal/upstream/manifest.json

